bind 0.0.0.0
# Password is injected via --requirepass flag in docker-compose.yml command.
# Do NOT add requirepass here — it would conflict and cause authentication errors.
maxmemory 512mb
maxmemory-policy noeviction
appendonly yes
appendfsync everysec
save 900 1
save 300 10
save 60 10000
